# ─── School examples (teacher reference files, not game code) ───────────────
Beipielarbeiten_U-Erkert/

# School documents — keep handout + submission PDF, ignore the rest
Dokumente_Schule/Submission/unpacked_check/
Dokumente_Schule/Submission/*.docx
Dokumente_Schule/Templates/

# ─── GSD planning (private — not for public repo) ───────────────────────────
.planning/

# ─── Claude Code local settings ─────────────────────────────────────────────
.claude/settings.local.json

# ─── Raw / uncompressed audio (too large for git — convert to MP3/OGG first) ─
# NOTE: .mp3 and .ogg ARE committed — the game needs them for GitHub Pages
*.wav
*.aif
*.aiff
*.flac

# ─── Video files (too large for git) ─────────────────────────────────────────
*.mp4
*.mov
*.avi
*.mkv
*.webm

# ─── Asset source / raw files (exports get committed, sources do not) ─────────
# Aseprite source files
*.aseprite
*.ase

# Audio project files (DAW sources)
*.aup
*.aup3
*.flp
*.ptx
*.logic

# Photoshop / GIMP sources
*.psd
*.xcf

# Temp / scratch folders
temp/
tmp/
scratch/
*.tmp

# ─── OS noise ────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ─── Editor ──────────────────────────────────────────────────────────────────
.vscode/
*.swp
*~

# ─── Repo-Polish Cleanup (2026-04-29) ────────────────────────────────────────
.claude/
Visuals/_archive/
Visuals/_dev/Inspiration/
Visuals/fonts/Archive/
docs/Screenshots/Screenshot*.png
# Wir behalten zwei .ai Sources die als Quellen aktiv im Game/README referenziert sind:
#   Visuals/fonts/alphabet_pixel_retro_video_game_style.ai (YELLOW_FONT atlas)
#   Visuals/thought_bubbles/dialogue_bubbles.ai            (8 bubble PNGs)
